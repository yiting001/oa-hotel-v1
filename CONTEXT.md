# OA Hotel Domain Context

## 平台语言

- **组织**：租户内的部门树、岗位和人员任职关系。
- **部门**：有父子层级、负责人、状态和有效期的组织节点。
- **岗位**：人员在某个部门中承担的组织职责，不等同于权限角色。
- **任职**：用户与部门、岗位的有效关系。一个用户可以有多个任职，但只能有一个主任职。
- **角色**：功能权限集合，可以按数据范围授予用户，不直接等同于某个人。
- **权限**：稳定的功能编码，用于服务端操作授权和前端菜单、按钮可见性。
- **数据范围**：角色权限可作用的数据集，包括本人、指定部门、部门树和全部。
- **表单定义**：稳定的表单身份，包含多个不可变的表单版本。
- **表单版本**：某次发布的字段、布局、校验、节点字段权限和打印结构快照。发布后不可修改。
- **流程定义**：稳定的审批流程身份，包含多个不可变的流程版本。
- **流程版本**：节点、连线、办理人规则、动作和字段权限的发布快照。运行中的实例始终绑定原版本。
- **业务单据**：合同、印章、物资等业务模块拥有的领域数据。
- **任务候选人**：流程节点创建时根据组织、角色和单据上下文解析后固化的可办理用户。
- **A4 打印模板**：独立于屏幕填写布局的纸张结构，定义方向、边距、网格、分页、附件和审批意见。
- **归档包**：单据快照、表单版本、流程轨迹、意见、附件清单和打印文件的不可变集合。

## 不变量

1. 已发布表单和流程版本不可原地修改。
2. 任务只能由创建时固化的候选人办理，不能只凭全局角色字符串判断。
3. 功能权限可做并集，但每个角色的数据范围必须与该角色授权绑定，不得被其他角色扩大。
4. 屏幕表单与 A4 打印模板可共享字段，但不共享布局。
5. 前端隐藏菜单、按钮和字段不能替代服务端授权。
